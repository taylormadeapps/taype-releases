# Фрагменти (MCP)

Керування фрагментами TayPE через MCP.

## Команди

- `add_clip <track> <path> <time>` - додайте аудіо-фрагмент
- `remove_clip <id>` - видаліть фрагмент
- `set_clip_time <id> <position>` - змініть часову позицію
- `set_clip_gain <id> <gain>` - встановіть посилення фрагменту
