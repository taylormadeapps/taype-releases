# Плагіни (MCP)

Керування плагінами через MCP.

## Команди

- `load_plugin <track> <slot> <plugin>` - завантажте плагін
- `remove_plugin <track> <slot>` - видаліть плагін
- `set_plugin_param <track> <slot> <param> <value>` - встановіть параметр плагіна
