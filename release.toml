# Machine-readable release metadata.
# Updated during checkpoints and releases.
# Consumed by LLMs, CI systems, and tooling.

[release]
version = "0.21.0"
date = "2026-06-02"
commit = "cc993cee"

summary = """\
Enabled TONE3000 A2 NAM support.
Added beta-aware update checking with Google Drive beta build discovery, release-note buttons, and an opt-in startup update check that stays silent when no update is available.
Expanded playback-safe editing so allowed timeline, channel-strip, routing, preamp/summing, and plugin graph changes can continue during playback, while denied reel/media/export/device actions stop playback before continuing and recording remains protected.
Added bus-DAG preamp and SUM worker grouping for virtual-console workflows, with a clearer DSP monitor grouping view and default-on NAM AMX behaviour for fresh configurations.
Fixed direct insert drag/drop so moves onto empty slots or existing inserts preserve sparse slot positions and work correctly on tracks, busses, and the master strip.
"""

# Verbatim git log from previous stable release tag to this checkpoint.
# Raw commit history for traceability.
git_log = """\
cef2aa25 Fix insert slot drag targets
0539ee04 Add beta update checks and test cache seeding
640b90d5 Respect test app support for license state
6734534d Stop playback for transport-denied UI actions
7a9fe102 Add beta DSP grouping and harness state restore
ae968949 Add Republic of TayPE logo assets
2ac7a68e Stabilize flaky UI and restart tests
"""
